# Informal-data-detailing-orders.
The dataset consists of multiple tables and columns containing information such as products, order dates, order amounts, regions, cities, and so on. Specific data was retrieved from it using SQLite commands such as `WHERE`, `ORDER BY`, and `LIMIT`.
